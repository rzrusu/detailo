# Configuration repository path for config-server
# Defaults to ./config-repo (relative to docker-compose.yml location)
# Override this if your config repository is located elsewhere
CONFIG_REPO=./config-repo

# OAuth Client Secret for identity-service
# IMPORTANT: Set this to a secure, randomly generated secret
# Never commit actual secrets to source control
# Generate a secure secret using: openssl rand -base64 32
DETAILO_OAUTH_CLIENT_SECRET=your-secret-here
